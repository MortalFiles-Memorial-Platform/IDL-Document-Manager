services:
  - type: web
    name: idl-ris
    runtime: node
    buildCommand: npm run build
    startCommand: npm run db:migrate:deploy && npm run start
    envVars:
      - key: NODE_ENV
        value: production
